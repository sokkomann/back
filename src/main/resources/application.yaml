server:
  port: 10000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/haetssal_jangteo?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: haetssal_jangteo
    password: 1234

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.app.haetssal_jangteo.domain; com.app.haetssal_jangteo.dto;
  type-handlers-package: com.app.haetssal_jangteo.mybatis.handler