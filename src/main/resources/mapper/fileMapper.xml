<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.haetssal_jangteo.mapper.FileMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_file (file_type, file_name, file_origin_name, file_saved_path, file_size)
        values (#{fileType}, #{fileName}, #{fileOriginName}, #{fileSavedPath}, #{fileSize});
    </insert>

    <delete id="delete">
        delete from tbl_file
        where id = #{id}
    </delete>
</mapper>
