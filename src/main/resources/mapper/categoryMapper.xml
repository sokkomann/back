<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.haetssal_jangteo.mapper.CategoryMapper">
    <insert id="insert">
        insert into tbl_category (id, category_name)
        values (#{id}, #{categoryName})
    </insert>

    <insert id="insertSub">
        insert into tbl_sub_category (id, category_name, parent_category_id)
        values (#{id}, #{categoryName}, #{parentCategoryId})
    </insert>

    <delete id="delete">
        delete from tbl_category
        where id = #{id}
    </delete>

    <delete id="deleteSub">
        delete from tbl_sub_category
        where id = #{id}
    </delete>

    <select id="selectCateAll">
        select id, category_name
        from tbl_category
    </select>

    <select id="selectOneById">
        select id, category_name
        from tbl_category
        where id = #{id}
    </select>

    <select id="selectByParentId">
        select id, category_name, parent_category_id
        from tbl_sub_category
        where parent_category_id = #{parentCategoryId}
    </select>

    <select id="selectSubOneById">
        select id, category_name, parent_category_id
        from tbl_sub_category
        where id = #{id}
    </select>
</mapper>